{
  "name": "sandbox",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b && vite build",
    "dev": "vite --host 127.0.0.1",
    "test": "vitest run --root ../.. --config vitest.config.ts apps/sandbox/src --passWithNoTests",
    "lint": "eslint ."
  },
  "dependencies": {
    "@gamekit/core": "workspace:*",
    "@gamekit/event-bus": "workspace:*",
    "@gamekit/game-runtime": "workspace:*",
    "@gamekit/world": "workspace:*",
    "@gamekit/world-koota": "workspace:*"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "vitest": "^3.1.3"
  }
}
